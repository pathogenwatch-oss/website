export const DELAY = 64;
export const FONT_SIZE = 14;
export const HALO_WIDTH = 4;
export const MAX_FONT_SIZE = 64;
export const MAX_NODE_SIZE = 64;
export const MIN_FONT_SIZE = 1;
export const MIN_NODE_SIZE = 1;
export const NODE_BORDER_STYLE = 'rgba(0, 0, 0, 0.54)';
export const NODE_BORDER_WIDTH = 1;
export const NODE_RADIUS = 7;
export const NODE_SIZE = 14;
export const FONT = '"Roboto", "Helvetica", "Arial", sans-serif';

export default {
  DELAY,
  FONT_SIZE,
  HALO_WIDTH,
  MAX_FONT_SIZE,
  MAX_NODE_SIZE,
  MIN_FONT_SIZE,
  MIN_NODE_SIZE,
  NODE_BORDER_STYLE,
  NODE_BORDER_WIDTH,
  NODE_RADIUS,
  NODE_SIZE,
  FONT,
};
